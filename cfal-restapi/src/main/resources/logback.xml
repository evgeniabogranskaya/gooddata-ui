<?xml version="1.0" encoding="UTF-8"?>

<configuration>

    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter" />
    <conversionRule conversionWord="wex" converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter" />

    <property name="CONSOLE_LOG_PATTERN" value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n%wex"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml" />

    <jmxConfigurator/>

    <!-- default appender -->
    <appender name="SYSLOG" class="com.gooddata.log.GdcSyslogAppender">
        <facility>local2</facility>
        <syslogHost>localhost</syslogHost>
        <suffixPattern>
            %d{yyyy-MM-dd'T'HH:mm:ss.SSSZ} %t %c %p: %m component=cfal_restapi request_id=%X{requestId} auth_user=%X{userId} impersonator=%X{impersonatorId} %n %xEx{50}
        </suffixPattern>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="SYSLOG" />
    </root>

</configuration>